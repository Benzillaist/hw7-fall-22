/**
 * Fetches the approximate longitude and latitude of a specified location
 *
 * @param {string} query A name of a location
 * @return {Promise<object>} Returns a object with two fields "lon" and "lat" 
 * with the respective longitude and latitude of 
 */
export function fetchLongitudeAndLatitude(query) {}
